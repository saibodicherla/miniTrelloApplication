import React from "react";
import Styled from "../../styles/Header";

function Header() {
  return (
    <Styled.HeaderContainer>
      <Styled.HeaderLogo>Mini-Trello Board</Styled.HeaderLogo>
    </Styled.HeaderContainer>
  );
}

export default Header;
